# Owned by claude-docs, not this repo — see claude-docs/CLAUDE.md
# ("Nested project code repos")
RESEARCH.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local config / secrets
.env
*.env
data/

# OS
.DS_Store

# Connect IQ build output
watch/bin/
*.prg
*.prg.debug.xml
*.iq
watch/bin/*.iq

# Hand-offs are the exception: a tester build has to be reachable from a
# machine that is not this one, so it is tracked deliberately rather than
# force-added past the rules above.
!dist/
!dist/**
